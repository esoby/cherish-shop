export type SignInFormFields = {
  email: string;
  password: string;
};

export type SignUpFormFields = {
  email: string;
  name: string;
  password: string;
  pwdCheck: string;
  isSeller: boolean;
};
